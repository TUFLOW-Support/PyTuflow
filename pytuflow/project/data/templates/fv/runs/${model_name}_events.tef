! TUFLOW EVENT FILE (.TEF) defines the parameters for the model events

Define Event == ${event_name}				        ! Start definition of block of commands
	BC Event Source == ~event~ | ${event_text}		! Specifies the text in which '~event~' is to be substituted within the bc_dbase
End Define